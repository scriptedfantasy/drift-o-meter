/**
 * @deprecated Moved to `src/ui/parts.tsx`.
 *
 * `SectionHead`, `Stat` and `Tag` are used by the garage and the calibration screen as
 * well as by this one, so they are no longer part of the results kit. This re-export
 * keeps the old import path working while the screens move over; delete it after.
 */
export { SectionHead, Stat, Tag } from '../parts';
